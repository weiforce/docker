#!/bin/bash

if [ ! $1 ]
then
    echo "command: dante_config url"
    echo "sudo wget http://server/config"
    exit
fi

cd ~/data/dante
sudo curl -o passwd ${url}passwd
sudo curl -o shadow ${url}shadow
