[Unit]
Description=mysql
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStartPre=-/usr/bin/docker kill mysql
ExecStartPre=-/usr/bin/docker rm mysql
ExecStartPre=/usr/bin/docker pull mysql:5.6
ExecStart=/usr/bin/docker run --name mysql -v /home/core/data/mysql:/etc/mysql -v /home/core/data/mysql/data:/var/lib/mysql -e MYSQL_ROOT_PASSWORD=yellow --user=root mysql:5.6
ExecStop=/usr/bin/docker kill mysql

[Install]
WantedBy=multi-user.target
